import React from 'react'
import styles from './style.scss'

const Typography__Subtle_Title = (props) => (
  <h4 className={styles.typography__subtle_title}>
    {props.text}
  </h4>
)

export default Typography__Subtle_Title
