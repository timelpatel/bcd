import React from 'react'
import styles from './style.scss'

const Container__Billboard = (props) => (

  <div className={styles.container__billboard}>
    {props.children}
  </div>

)

export default Container__Billboard
