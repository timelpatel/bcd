import React from 'react'
import styles from './style.scss'

const Imagery__Example = (props) => (

  <div
    className={styles.imagery__example}
  >
    {props.children}
  </div>

)

export default Imagery__Example
