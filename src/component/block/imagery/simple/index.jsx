import React from 'react'
import styles from './style.scss'

const Imagery__Simple = (props) => (

  <div
    className={styles.imagery__simple}
  >
    {props.children}
  </div>

)

export default Imagery__Simple
